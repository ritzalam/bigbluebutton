package org.bigbluebutton.common.signal
{
  import org.osflash.signals.Signal;
  
  public class ConnectSuccessSignal extends Signal
  {
    public function ConnectSuccessSignal(...parameters)
    {
      super(parameters);
    }
  }
}